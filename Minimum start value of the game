#include <stdio.h>
int main()
{
int nofgame,sum=0,amt=0;
scanf("%d",&nofgame);
int game[nofgame];
for(int i=0;i<nofgame;i++)
    scanf("%d",&game[i]);
for(int i=0;i<nofgame;i++)
    sum+=game[i];
    if(sum<1)
    {
        amt=1-sum;
        sum=1;

    }
printf("\n%d",amt);
    return 0;
}


